#include <stdbool.h>
#include "colors.h"

int main()
{
    while (true)
    {
        set_background(WHITE);
        set_background(BLUE);
        set_background(GREEN);
        set_background(CYAN);
        set_background(RED);
        set_background(MAGENTA);
        set_background(BROWN);
        set_background(LIGHT_GRAY);
        set_background(GRAY);
        set_background(LIGHT_BLUE);
        set_background(LIGHT_GREEN);
        set_background(LIGHT_CYAN);
        set_background(LIGHT_RED);
        set_background(LIGHT_MAGENTA);
        set_background(YELLOW);
        set_background(BLACK);
    }
    return 0;
}
